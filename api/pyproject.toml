[project]
name = "mgz-pkmn-api"
version = "0.1.0"
description = "FastAPI service for the mgz-pkmn card lookup tool."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.46.0",
    "mgz-pkmn",
]

[tool.uv.sources]
mgz-pkmn = { path = "..", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
